'use strict';

/**
 * @ngdoc function
 * @name frontendApp.controller:TimelineCtrl
 * @description
 * # TimelineCtrl
 * Controller of the frontendApp
 */
angular.module('frontendApp')
  .controller('TimelineCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
